% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_calibrate.R
\name{fev_exposure_calibrate}
\alias{fev_exposure_calibrate}
\title{Fit the exposure radius to local fires}
\usage{
fev_exposure_calibrate(
  fuel,
  burnt_areas,
  radii = NULL,
  type = "ember",
  millesime = NULL,
  max_lag_years = NULL,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{fuel}{A \link{fev_fuel_source}, \code{fev_layer} or \code{SpatRaster}, as
\code{\link[=fev_exposure]{fev_exposure()}} takes.}

\item{burnt_areas}{Observed fires: a \link{fev_source} from \code{\link[=fev_fetch_burnt]{fev_fetch_burnt()}},
an \code{sf} or a \code{SpatVector}.}

\item{radii}{Radii to try, in CRS units. \code{NULL} uses a geometric sweep from
three cells to a tenth of the smaller extent side, which spans the
published scales without wandering past what the layer can support.}

\item{type}{Passed to \code{\link[=fev_exposure]{fev_exposure()}} for provenance only; the radius is
what varies here.}

\item{millesime}{Fuel vintage for the temporal check. \code{NULL} digs it out of
the provenance, as \code{\link[=fev_validate]{fev_validate()}} does.}

\item{max_lag_years}{Passed to the temporal check. \code{NULL}, the default and
the same as \code{\link[=fev_validate]{fev_validate()}}, reports the bias without filtering on it.
Setting it without a known vintage is an error there and here: there would
be nothing to compare the fire dates against.}

\item{quiet}{Suppress progress and the standing caveats.}

\item{...}{Passed to \code{\link[=fev_exposure]{fev_exposure()}}, so \code{no_burn}, \code{na_rm} and \code{trim} apply
identically at every radius.}
}
\value{
An object of class \code{fev_exposure_calibration}: a list with \code{table}
(one row per radius: \code{radius}, \code{reachable}, \code{auc}, \code{n_burnt}, \code{note}),
\code{best} (the radius of maximum AUC, \code{NA} if none was reachable), \code{at_edge}
(whether that best sits on the first or last radius tried, in which case it
is the edge of the sweep rather than an optimum), \code{temporal} and
\code{provenance}.
}
\description{
Sweeps a series of radii, computes \code{\link[=fev_exposure]{fev_exposure()}} at each, and scores each
against observed fires with the same AUC \code{\link[=fev_validate]{fev_validate()}} uses. Returns the
whole sweep, not only the winner, because the shape of the curve says more
than its maximum: a flat curve means the radius hardly matters on these
fuels, and a sharp one means it does.
}
\section{This is a fit, not a validation}{

The radius that maximises AUC is the radius that best separates burnt from
unburnt \strong{in the fires you supplied}, with nothing held out to test it on.
Report it as a fitted parameter. A radius fitted on eleven fires in one massif
and then quoted as validated would be worse than the Canadian default it
replaces, because it would carry a local authority it has not earned.

The honest use is the one the package's own limitation calls for: replacing
\emph{"30, 100 and 500 m, from Alberta"} with \emph{"250 m, fitted on the Maures fires
of 2003-2021, AUC 0.71"} — a statement whose provenance a reader can weigh.
}

\section{The temporal bias applies here too, and harder}{

\code{\link[=fev_validate]{fev_validate()}} warns when fires postdate the fuel vintage. Fitting on such
a sample does not merely describe a biased situation, it \strong{bakes the bias
into a parameter}. The check runs first and its verdict travels with the
result.
}

\section{Radii out of reach are declared, never dropped}{

\code{\link[=fev_exposure]{fev_exposure()}} refuses \code{res > radius / 3}. At 25 m every radius below 75 m
is unreachable, which on the shipped defaults removes the radiant-heat scale
entirely. Those radii come back with \code{reachable = FALSE} and no AUC rather
than being quietly left out of the sweep, because a table that silently
contains only what happened to work reads as if it had covered everything.
}

\examples{
\dontrun{
zone <- sf::st_read(system.file("extdata", "maures.gpkg",
                                package = "firexpovulnR"), "study_area")
feux <- sf::st_read(system.file("extdata", "maures.gpkg",
                                package = "firexpovulnR"), "burnt_areas")
cal <- fev_exposure_calibrate(combustible, feux,
                              radii = c(100, 250, 500, 1000))
cal
plot(cal)
}

}
\seealso{
\code{\link[=fev_exposure]{fev_exposure()}}, \code{\link[=fev_exposure_radii]{fev_exposure_radii()}} for the shipped values and
where they come from, \code{\link[=fev_validate]{fev_validate()}} for the AUC itself.
}
