% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuel_source.R
\name{fev_fuel_source}
\alias{fev_fuel_source}
\title{Build a fuel source}
\usage{
fev_fuel_source(
  x,
  type = c("bdforet_v2", "clc_2018", "clcplus_2023", "lidarhd", "custom"),
  res = 25,
  crs_work = 2154,
  field = NULL,
  lookup = NULL,
  millesime = NULL,
  aoi = NULL,
  touches = FALSE,
  register = c("auto", "categorical", "continuous"),
  units = NULL,
  provenance = NULL
)
}
\arguments{
\item{x}{A \link{fev_source} from \code{\link[=fev_fetch_bdforet]{fev_fetch_bdforet()}} or \code{\link[=fev_fetch_corine]{fev_fetch_corine()}},
or a plain \code{sf}, \code{SpatVector} or \code{SpatRaster}.}

\item{type}{Source type: \code{"bdforet_v2"}, \code{"clc_2018"} (and the other CORINE
vintages), \code{"clcplus_2023"} (and 2018, 2021), \code{"lidarhd"} (phase 8, not
implemented) or \code{"custom"}.}

\item{res}{Target cell size in CRS units, metres for the projected
defaults. See the resolution section.}

\item{crs_work}{EPSG code of the working CRS. Default \code{2154}, BD Forêt's
native CRS. A categorical source is \strong{not} reprojected silently: when
\code{crs_work} differs from the data's CRS the reprojection is warned about
and logged, because nearest-neighbour on a class layer displaces
boundaries.}

\item{field}{Name of the class column. Defaults to \code{code_tfv} for BD Forêt
and to the \code{code_XX} column CORINE serves for its vintage.}

\item{lookup}{Correspondence table. Defaults to the shipped one for \code{type};
pass your own from \code{\link[=fev_fuel_lookup]{fev_fuel_lookup()}} to override it.}

\item{millesime}{Vintage of the data. See the vintage section. \code{NULL} takes
it from \code{x}'s provenance record when there is one.}

\item{aoi}{Optional area of interest to crop the grid to.}

\item{touches}{Rasterisation rule for polygons. See the section above.}

\item{register}{For \code{type = "custom"} with a \code{SpatRaster}: which register
the layers populate. \code{"auto"} reads a categorical raster as categorical
and a plain numeric one as continuous.}

\item{units}{Named character vector of units for continuous layers, e.g.
\code{c(cbd = "kg/m3")}. Recorded in the provenance and printed.}

\item{provenance}{Provenance record to carry forward. Taken from \code{x} when
\code{NULL}.}
}
\value{
An object of class \code{fev_fuel_source}.
}
\description{
Turns a fetched land cover dataset into a fuel source on a defined grid: a
categorical class layer, the correspondence table that gives those classes
fire meaning, and the provenance to say where they came from and for which
vintage.
}
\section{The two registers}{

A fuel source carries a \strong{categorical} register (a class code per pixel)
and a \strong{continuous} register (named numeric metrics per pixel). BD Forêt
and CORINE populate the first. The second exists so that continuous fuel
descriptions — LiDAR-derived load, bulk density, crown base height — can be
carried by the same object without the abstraction assuming everything is a
class. Use \code{\link[=fev_fuel_registers]{fev_fuel_registers()}} to see which are populated.
}

\section{On the target resolution}{

\code{res} defaults to 25 m, and the floor below which it buys nothing is a
property of the source, not a constant: BD Forêt v2's minimum mapped width is
20 m, CLCplus Backbone's is 10 m. Below its source's own width a grid
resolves boundaries the data never had. At 100 m the advantage over CORINE —
whose native raster is 100 m — disappears entirely. Between those, the cost
is quadratic in memory and in focal-window time.

One resolution earns its cost for a reason unrelated to detail. \code{fev_exposure()}
requires \code{res <= radius / 3}, so the 30 m radiant-heat radius is \strong{refused}
at 25 m and becomes computable at 10 m — exactly, since 10 = 30 / 3. A 10 m
source is therefore the only way to reach that scale at all, and it costs
about 39 times the focal work of 25 m: 6.25 times the cells, each with 6.25
times the window.
}

\section{On the vintage}{

For \code{type = "bdforet_v2"} the vintage is \strong{required}. BD Forêt v2 was built
department by department between 2007 and 2018, so a stand mapped in 2008
may have burnt in 2010 and be in another state entirely; without the vintage
the temporal-bias check in \code{fev_validate()} cannot run at all. The WFS does
not serve it and no per-department table was found (phase 2 report, section
2 bis), so it has to come from you. Passing \code{millesime = NA} explicitly is
accepted — it records "we looked and do not know", which is honest — but it
is not the default, so that the check is never skipped by accident.
}

\section{Rasterising polygons}{

\code{touches = FALSE} (the default) assigns a cell to a polygon only when the
cell centre falls inside it, which is the standard convention and keeps
burnable area unbiased. It does drop polygons narrower than the cell — at
25 m that means some of BD Forêt's 20 m minimum-width features. \code{touches = TRUE} keeps them, at the cost of inflating every class's area and letting
thin non-burnable features (a road strip) claim whole cells. Neither is
free; the choice is recorded in the provenance.
}

\examples{
# A miniature BD Forêt-style polygon layer.
poly <- sf::st_sf(
  code_tfv = c("FF2-57-57", "LA4"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(cbind(c(0, 100, 100, 0, 0), c(0, 0, 100, 100, 0)))),
    sf::st_polygon(list(cbind(c(100, 200, 200, 100, 100), c(0, 0, 100, 100, 0)))),
    crs = 2154
  )
)
fuel <- fev_fuel_source(poly, type = "bdforet_v2", res = 25, millesime = 2014)
fuel

}
\seealso{
\code{\link[=fev_fuel_merge]{fev_fuel_merge()}} to combine a primary and an auxiliary source,
\code{\link[=fev_fuel_binary]{fev_fuel_binary()}}, \code{\link[=fev_fuel_type]{fev_fuel_type()}} and \code{\link[=fev_fuel_availability]{fev_fuel_availability()}} for
the layers derived from one.
}
