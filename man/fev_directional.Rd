% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{fev_directional}
\alias{fev_directional}
\title{Directional vulnerability from a point}
\source{
Beverly, J.L., Forbes, A.M. (2023). Assessing directional vulnerability to
wildfire. \emph{Natural Hazards} 117: 831-849. Parameters verified against the
\code{fireexposuR} 1.2.0 reference documentation on 2026-08-16.
}
\usage{
fev_directional(
  exposure,
  point,
  seg_lengths = c(5000, 5000, 5000),
  interval = 1,
  n_wedges = NULL,
  max_dist = NULL,
  thresh_exp = 0.6,
  thresh_viable = 0.8,
  step = NULL
)
}
\arguments{
\item{exposure}{A \code{fev_exposure_layer} from \code{\link[=fev_exposure]{fev_exposure()}}, or a
\code{SpatRaster} of exposure values in \verb{[0, 1]}.}

\item{point}{The value at risk: an \code{sf}, \code{sfc}, \code{SpatVector} of one point,
or a length-2 numeric \code{c(x, y)} in the exposure layer's CRS.}

\item{seg_lengths}{Segment lengths outward from the point, in CRS units.}

\item{interval}{Degrees between transects.}

\item{n_wedges}{Alternative to \code{interval}: number of evenly spaced
directions, so \code{interval = 360 / n_wedges}.}

\item{max_dist}{Total transect length. When given, overrides \code{seg_lengths}
by splitting it into three equal segments.}

\item{thresh_exp}{Exposure at or above which ground counts as hazardous.}

\item{thresh_viable}{Share of a segment that must be hazardous for it to be
viable.}

\item{step}{Sampling interval along a transect. Defaults to the cell size.}
}
\value{
An object of class \code{fev_directional}: a list with \code{table} (one row
per bearing, one logical column per segment plus \code{viable} for all
segments), the parameters used, and a provenance record.
}
\description{
Draws transects outward from a value in every direction and reports which
bearings offer a continuous high-exposure pathway to it. The answer to
"where would a fire come from" rather than "how much fuel is around".
}
\section{Method}{

Follows Beverly and Forbes (2023). From the value, a transect is drawn at
every \code{interval} degrees. Each transect is cut into consecutive segments —
by default three of 5 km — and a segment counts as \strong{viable} when at least
\code{thresh_viable} of the ground it crosses has exposure of at least
\code{thresh_exp}. A direction with all three segments viable is a continuous
corridor of hazardous fuel reaching the value from that bearing.

Bearings are compass bearings: 0 and 360 are north, increasing clockwise.
}

\section{The two thresholds are measured, not conventional}{

\code{thresh_exp = 0.6} because observed fires burned preferentially where
exposure exceeded 60\%; \code{thresh_viable = 0.8} because observed burned
pathways intersected pre-fire high-exposure patches by 80\% on average. Both
from Canadian landscapes — see \code{\link[=fev_directional_defaults]{fev_directional_defaults()}}.
}

\examples{
r <- terra::rast(nrows = 60, ncols = 60, xmin = 0, xmax = 6000,
                 ymin = 0, ymax = 6000, crs = "EPSG:2154")
terra::values(r) <- 0
r[1:30, ] <- 0.9          # a high-exposure block to the north
d <- fev_directional(r, point = c(3000, 3000), seg_lengths = c(500, 500, 500),
                     interval = 45)
d$table

}
\seealso{
\code{\link[=fev_exposure]{fev_exposure()}}, \code{\link[=fev_directional_defaults]{fev_directional_defaults()}}.
}
