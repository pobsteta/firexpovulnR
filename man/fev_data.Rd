% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fev_source.R
\name{fev_data}
\alias{fev_data}
\alias{fev_source}
\alias{fev_source_info}
\title{The \code{fev_source} class, and how to get at its contents}
\usage{
fev_data(x)

fev_source_info(x)
}
\arguments{
\item{x}{A \code{fev_source}, or — for \code{fev_data()} — a \code{fev_fuel_layer} as
returned by \code{\link[=fev_fuel_binary]{fev_fuel_binary()}}, \code{\link[=fev_fuel_type]{fev_fuel_type()}} and
\code{\link[=fev_fuel_availability]{fev_fuel_availability()}}, which wrap their raster for the same reason.}
}
\value{
\code{fev_data()} returns the underlying \code{sf} or \code{SpatRaster}.
\code{fev_source_info()} returns the record as a named list.
}
\description{
\verb{fev_fetch_*()} functions return a \code{fev_source}: the spatial data together
with the provenance record that says where it came from — dataset,
provider, endpoint, exact request, vintage and download time. These
accessors pull out one or the other.
}
\details{
Data and record are held in one S3 object rather than as attributes on the
\code{sf}/\code{SpatRaster}, because \code{SpatRaster} is S4 and drops user attributes at
the first arithmetic operation — the record would vanish silently on first
use.
}
\examples{
# Offline example: build a source object by hand.
r <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
terra::values(r) <- seq_len(16)
s <- firexpovulnR:::new_fev_source(r, dataset = "demo", provider = "none")
fev_source_info(s)$dataset

}
