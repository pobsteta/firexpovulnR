% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vulnerability.R
\name{fev_vuln_stack}
\alias{fev_vuln_stack}
\title{Combine vulnerability dimensions}
\usage{
fev_vuln_stack(..., weights = NULL, method = c("weighted_mean", "max"))
}
\arguments{
\item{...}{Named layers: \code{fev_vuln_layer} objects, \code{fev_layer}, or
\code{SpatRaster} with values in \verb{[0, 1]}.}

\item{weights}{Numeric vector, one per layer, summing to 1. \code{NULL} gives
equal weights and says so.}

\item{method}{\code{"weighted_mean"} (default) or \code{"max"}, which takes the worst
dimension rather than trading them off.}
}
\value{
A \code{fev_vuln_layer} named \code{vulnerability}.
}
\description{
Aggregates several normalised layers — human, economic, ecological — into
one, by weighted mean.
}
\section{On the weights}{

They are the analysis's value judgement, not a property of the data: setting
human vulnerability to 0.6 and ecological to 0.4 is a statement about what
matters, and there is no data-driven answer to it. The package has no
default beyond equal weights, refuses weights that do not sum to 1, and
writes them into the provenance so that a reader of the result can see the
judgement that produced it.
}

\section{Grids must already match}{

As everywhere else outside \code{\link[=fev_align]{fev_align()}}, mismatched grids are an error.
}

\examples{
g <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
human <- terra::setValues(g, seq(0, 1, length.out = 16))
eco <- terra::setValues(g, rev(seq(0, 1, length.out = 16)))
v <- fev_vuln_stack(human = human, eco = eco, weights = c(0.6, 0.4))
terra::global(fev_data(v), "mean", na.rm = TRUE)

}
\seealso{
\code{\link[=fev_vuln_layer]{fev_vuln_layer()}}, \code{\link[=fev_align]{fev_align()}}.
}
