% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fev_stack.R
\name{fev_stack_save}
\alias{fev_stack_save}
\alias{fev_stack_read}
\title{Save and reload a \code{fev_stack}}
\usage{
fev_stack_save(x, file)

fev_stack_read(file)
}
\arguments{
\item{x}{A \code{fev_stack}.}

\item{file}{Path to an \code{.rds} file.}
}
\value{
\code{fev_stack_save()} returns \code{file} invisibly;
\code{fev_stack_read()} returns a \code{fev_stack}.
}
\description{
\code{terra} rasters hold a C++ pointer, so a plain \code{\link[base:saveRDS]{base::saveRDS()}} of a
\code{fev_stack} writes an object whose layers are unusable when reloaded. These two
functions wrap the layers with \code{\link[terra:wrap]{terra::wrap()}} first, so the object -- and
the provenance record travelling with it -- survives a round trip to disk.
}
\details{
Large in-memory rasters are materialised by wrapping. For heavy layers,
write the rasters to GeoTIFF separately and keep only the provenance here.
}
\examples{
r <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
terra::values(r) <- seq_len(16)
s <- fev_stack(fuel = r, crs_work = 2154)
f <- tempfile(fileext = ".rds")
fev_stack_save(s, f)
s2 <- fev_stack_read(f)
names(s2)

}
