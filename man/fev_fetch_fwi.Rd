% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_fwi.R
\name{fev_fetch_fwi}
\alias{fev_fetch_fwi}
\title{Fetch CEMS fire danger indices}
\usage{
fev_fetch_fwi(
  aoi,
  period,
  product = c("fire_weather_index", "fine_fuel_moisture_code", "duff_moisture_code",
    "drought_code", "initial_spread_index", "build_up_index",
    "fire_daily_severity_index"),
  grid = "0.25/0.25",
  dataset = "cems-fire-historical-v1",
  user = "ecmwfr",
  path = NULL,
  transfer = TRUE
)
}
\arguments{
\item{aoi}{Area of interest: \code{sf}, \code{sfc}, \code{bbox}, \code{SpatVector} or
\code{SpatRaster}. Must carry a CRS. Converted to a lon/lat bounding box for
the request.}

\item{period}{Two-element vector of dates or years bounding the request,
e.g. \code{c("1991-01-01", "2020-12-31")}.}

\item{product}{Character vector of indices to retrieve. Defaults to the
Canadian FWI system set.}

\item{grid}{Request grid as \code{"lat/lon"} degrees. Explicit on purpose.}

\item{dataset}{EWDS dataset identifier.}

\item{user}{\code{ecmwfr} user id.}

\item{path}{Directory for the downloaded file. Defaults to the package
cache.}

\item{transfer}{Perform the download. \code{FALSE} builds and returns the
request without contacting the service — useful to inspect exactly what
would be sent.}
}
\value{
A \link{fev_source} holding a \code{SpatRaster} of the requested indices.
When \code{transfer = FALSE}, the request list is returned instead.
}
\description{
Downloads Canadian Fire Weather Index System indices from the Copernicus
Emergency Management Service historical reconstruction, hosted on the Early
Warning Data Store (EWDS) and driven by ERA5 reanalysis.
}
\section{Credentials}{

The EWDS needs a personal Copernicus API token. The package never handles
it: it reads \code{ECMWF_KEY} from the environment (set it in \verb{~/.Renviron}), or
falls back to whatever \code{ecmwfr} already has in its keyring. \strong{Never put a
token in a script or commit one.}

\if{html}{\out{<div class="sourceCode">}}\preformatted{# in ~/.Renviron
ECMWF_KEY=your-token-here
}\if{html}{\out{</div>}}
}

\section{Resolution, and why it matters here more than elsewhere}{

The reanalysis is on a 0.25° grid — roughly 20-30 km at French latitudes.
Fuel-derived exposure is at 20-100 m. \strong{That is a ratio of several hundred},
and it is the central methodological difficulty of the whole chain. Nothing
in this function hides it: combining these layers with anything else goes
through \code{fev_align()}, which warns and records the ratio.

Note also that the EWDS request examples use \code{grid = "0.5/0.5"} while the
dataset overview announces 0.25° for the reanalysis. The discrepancy is
unresolved upstream, so \code{grid} is an explicit argument here rather than a
hidden default.
}

\section{Verification status}{

The dataset identifier, service name and variable names were read from the
EWDS documentation and from the \code{ecmwfr} source on 2026-08-15. \strong{No live
request has been made from this package}, because that requires a personal
token. Treat the first real call as part of your own validation, and check
that what comes back matches what you asked for.
}

\examples{
# Inspect the request that would be sent, without any credentials:
aoi <- sf::st_as_sfc(sf::st_bbox(
  c(xmin = 6.2, ymin = 43.1, xmax = 6.6, ymax = 43.4), crs = sf::st_crs(4326)
))
req <- fev_fetch_fwi(aoi, period = c("2020-01-01", "2020-12-31"),
                     transfer = FALSE)
req$dataset_short_name
req$variable

}
\seealso{
\code{fev_fwi_percentile()} to turn raw indices into regional
percentile ranks, which is what makes them comparable across climates.
}
