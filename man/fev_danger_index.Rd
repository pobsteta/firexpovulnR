% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/danger_index.R
\name{fev_danger_index}
\alias{fev_danger_index}
\title{Composite fire danger}
\usage{
fev_danger_index(
  danger,
  fuel_availability,
  method = c("product", "min", "mean", "geometric"),
  normalise = c("auto", "percentile", "minmax", "none"),
  weights = c(0.5, 0.5)
)
}
\arguments{
\item{danger}{Meteorological fire danger: a \code{fev_danger_layer} from
\code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}}, a \code{SpatRaster}, or a \link{fev_source}.}

\item{fuel_availability}{Fuel availability in \verb{[0, 1]}: a \code{fev_fuel_layer}
from \code{\link[=fev_fuel_availability]{fev_fuel_availability()}}, or a \code{SpatRaster}.}

\item{method}{\code{"product"}, \code{"min"}, \code{"mean"} or \code{"geometric"}.}

\item{normalise}{How to bring \code{danger} into \verb{[0, 1]}: \code{"auto"} (percentile
ranks only), \code{"percentile"}, \code{"minmax"}, or \code{"none"} when it is already
scaled.}

\item{weights}{Two weights for \code{method = "mean"}, danger then fuel. Must sum
to 1.}
}
\value{
A \code{fev_danger_layer} holding a \code{SpatRaster} named \code{danger} with
values in \verb{[0, 1]}.
}
\description{
Combines meteorological fire danger with fuel availability into one
normalised index in \verb{[0, 1]}. Weather that cannot reach fuel is not danger,
and fuel with no weather to dry it is not either.
}
\section{Both inputs must already share a grid}{

This function does not resample. Fire danger from ERA5 is kilometric and
fuel is decametric; bringing them together is a deliberate, recorded
operation that belongs to \code{fev_align()}. Passing mismatched grids here is an
error, not a convenience to smooth over.

\code{fev_align()} lands in phase 7. Until then, align the layers yourself and
say so in your methods — that is the same requirement, just manual.
}

\section{Normalising the danger side}{

\code{method} combines two numbers in \verb{[0, 1]}, so danger has to get there first.
A \code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}} result is already a percentile rank and is divided
by 100. Anything else needs \code{normalise} stated explicitly: the function will
not guess whether a layer of values between 0 and 60 is raw FWI, a
percentile, or something else entirely.
}

\section{Choosing a method}{

\code{"product"} is the default because fire needs both: either factor at zero
gives zero, which is the behaviour a fuel break or a wet week should have.
It is also the most severe — the product of two numbers below 1 is below
both — so a landscape of moderate danger over moderate fuel scores low.

\code{"min"} is the limiting-factor reading: danger is whatever is scarcest.
\code{"mean"} lets one factor compensate for the other, which is the EFFIS-style
additive logic and is the least defensible physically, though it is what
composite indices usually do. \code{"geometric"} sits between product and mean.

None of these is validated against burnt-area data here. That is
\code{fev_validate()}, in phase 7.
}

\examples{
g <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
d <- terra::setValues(g, seq(0, 100, length.out = 16))
a <- terra::setValues(g, rep(c(0, 0.5, 0.95, 1), 4))
idx <- fev_danger_index(d, a, normalise = "percentile")
terra::global(fev_data(idx), "range", na.rm = TRUE)

}
\seealso{
\code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}}, \code{\link[=fev_fuel_availability]{fev_fuel_availability()}}.
}
