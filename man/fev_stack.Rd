% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fev_stack.R
\name{fev_stack}
\alias{fev_stack}
\title{Create a \code{fev_stack}}
\usage{
fev_stack(..., crs_work = 2154, provenance = NULL)
}
\arguments{
\item{...}{Named \code{SpatRaster} objects, one per layer. Names become layer
names and must be unique and non-empty.}

\item{crs_work}{Integer EPSG code of the working CRS. Defaults to \code{2154}
(RGF93 / Lambert-93), the CRS of BD Forêt v2, the primary fuel source.
Use \code{3035} (ETRS89-LAEA) for CORINE-driven or multi-country studies.}

\item{provenance}{An existing provenance record to carry forward. When
\code{NULL} a fresh one is created.}
}
\value{
An object of class \code{fev_stack}: a named list of \code{SpatRaster} with a
\code{provenance} attribute.
}
\description{
A \code{fev_stack} bundles the raster layers of an analysis with the provenance
record that makes it replayable: sources and their vintages, the working
CRS and resolution, and an ordered log of every operation with the
parameters it used.
}
\details{
Layers are \strong{not} required to share a grid. Fire danger from ERA5 is
kilometric while fuel-derived exposure is decametric, and forcing them onto
one grid at construction time would hide a resampling step that has to be
explicit. Use \code{fev_align()} when layers must be combined.

All layers must, however, share a CRS. Mixing CRS in one object makes every
downstream extent comparison wrong in a way that is hard to see.
}
\examples{
r <- terra::rast(nrows = 10, ncols = 10, xmin = 0, xmax = 250,
                 ymin = 0, ymax = 250, crs = "EPSG:2154")
terra::values(r) <- runif(100)
s <- fev_stack(exposure = r, crs_work = 2154)
s

}
\seealso{
\code{\link[=fev_provenance]{fev_provenance()}} to export the record, \code{\link[=fev_check_crs]{fev_check_crs()}} to
validate inputs before building one.
}
