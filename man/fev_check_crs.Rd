% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_crs.R
\name{fev_check_crs}
\alias{fev_check_crs}
\title{Validate CRS and extents at the start of a chain}
\usage{
fev_check_crs(..., crs_work = 2154, primary = NULL, strict = TRUE)
}
\arguments{
\item{...}{Named spatial objects: \code{SpatRaster}, \code{SpatVector}, \code{sf} or
\code{sfc}. Names appear in the messages, so use meaningful ones.}

\item{crs_work}{Integer EPSG code of the intended working CRS. Default
\code{2154} (RGF93 / Lambert-93), matching BD Forêt v2.}

\item{primary}{Name of the input that is the primary fuel source, as a
string matching one of \code{...}. When given, a mismatch between its native
CRS and \code{crs_work} is reported prominently: reprojecting it is exactly
what the working-CRS rule exists to avoid.}

\item{strict}{When \code{TRUE} (default), a missing or geographic CRS is an
error. When \code{FALSE}, it is a warning and the check returns its findings
so the caller can decide.}
}
\value{
Invisibly, an object of class \code{fev_crs_check}: a list with
\code{inputs} (a data frame of one row per input: name, class, CRS label,
projected flag), \code{crs_work}, \code{primary}, and \code{issues} (a character vector
of problems found, empty when clean).
}
\description{
Checks that every input carries a projected CRS (not lon/lat), that the
working CRS matches the primary fuel source's native CRS, and that the
extents actually overlap. Run this before building a \code{\link[=fev_stack]{fev_stack()}}: each of
these failures otherwise surfaces much later as an empty result, which
reads like a data problem rather than a CRS problem.
}
\section{Choosing \code{crs_work}}{

Work in the projection of the layer driving your finest computation.
\itemize{
\item Primary source = BD Forêt v2 (IGN) → \code{crs_work = 2154}, the package
default.
\item Primary source = CORINE, or a multi-country study → \code{crs_work = 3035}
(ETRS89-LAEA), which is equal-area and therefore comparable across
regions.
}
}

\examples{
r <- terra::rast(nrows = 10, ncols = 10, xmin = 0, xmax = 250,
                 ymin = 0, ymax = 250, crs = "EPSG:2154")
terra::values(r) <- runif(100)
chk <- fev_check_crs(fuel = r, crs_work = 2154, primary = "fuel")
chk$issues

}
