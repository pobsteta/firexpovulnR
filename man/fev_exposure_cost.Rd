% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{fev_exposure_cost}
\alias{fev_exposure_cost}
\title{What a focal exposure pass will cost, before committing to it}
\usage{
fev_exposure_cost(x, res = NULL, radius = NULL, rate = 2e+08)
}
\arguments{
\item{x}{A fuel layer: \code{SpatRaster}, \link{fev_fuel_source} or \code{fev_layer}.}

\item{res}{Cell size to cost, in CRS units. \code{NULL} uses \code{x}'s own.}

\item{radius}{Radius in metres. \code{NULL} costs all three shipped radii.}

\item{rate}{Weighted operations per second, for the time estimate. The
default was measured on the machine this package was developed on; yours
will differ, which is why it is an argument and the result is called an
estimate.}
}
\value{
A data frame with one row per radius: \code{radius}, \code{res}, \code{reachable},
\code{window_cells}, \code{ring_cells}, \code{ops} and \code{seconds}.
}
\description{
Reports the size of the window and the weighted operation count for a fuel
grid, at its own resolution or at a hypothetical one. Exists because the cost
of this step grows as the \strong{fourth power} of the inverse cell size — both the
cell count and the window area scale as \code{res^-2} — so the step from 25 m to
10 m is not a 2.5-fold increase but roughly forty-fold, and that is worth
knowing before a run rather than during one.
}
\section{The other reason to ask}{

\code{fev_exposure()} requires \code{res <= radius / 3}, so a coarse grid does not
merely cost less, it puts the short radii out of reach entirely. At 25 m the
30 m radiant-heat radius is refused; at 10 m it passes, exactly, since
\code{10 = 30 / 3}. This function reports whether each radius is reachable at the
resolution asked about, which is the trade the cost has to be weighed against.
}

\examples{
r <- terra::rast(nrows = 100, ncols = 100, xmin = 0, xmax = 2500,
                 ymin = 0, ymax = 2500, crs = "EPSG:2154")
terra::values(r) <- 1

# What the three scales cost on this grid, and which are out of reach.
fev_exposure_cost(r)

# And what refining to 10 m would cost.
fev_exposure_cost(r, res = 10)

}
\seealso{
\code{\link[=fev_exposure]{fev_exposure()}}, \code{\link[=fev_exposure_radii]{fev_exposure_radii()}}.
}
