% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/danger_class.R
\name{fev_danger_class}
\alias{fev_danger_class}
\title{Classify fire danger into named classes}
\usage{
fev_danger_class(
  x,
  scheme = c("effis", "caliver_europe", "custom"),
  breaks = NULL,
  labels = NULL
)
}
\arguments{
\item{x}{A \code{SpatRaster} of Fire Weather Index values, a \link{fev_source} or a
\code{fev_layer}.}

\item{scheme}{\code{"effis"} (default), \code{"caliver_europe"}, or \code{"custom"} when
\code{breaks} is supplied.}

\item{breaks}{Five interior class boundaries, overriding \code{scheme}.
\code{\link[=fev_fwi_thresholds]{fev_fwi_thresholds()}} returns a set derived from your own record.}

\item{labels}{Class labels, one more than \code{breaks}.}
}
\value{
A \code{fev_danger_layer} holding a categorical \code{SpatRaster} named
\code{danger_class}.
}
\description{
Cuts a Fire Weather Index layer into danger classes, and records which
scheme's breaks were used.
}
\section{Why this is not just \code{terra::classify()}}{

The two published schemes disagree substantially — an FWI of 40 is
\emph{Very High} under the EFFIS operational breaks and \emph{Extreme} under the
caliver breaks derived from ERA-Interim. A classified map is a different map
under each, and nothing in a GeoTIFF says which one was applied. Here the
scheme, its breaks and its source go into the provenance record with the
result, so the question is answerable six months later.
}

\section{Classifying percentile ranks instead}{

If your layer is a \code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}} result, absolute FWI breaks are
meaningless for it and this function refuses. Percentile ranks are already
calibrated; cut them on percentile breaks of your own choosing, and say what
they are.
}

\examples{
r <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
terra::values(r) <- seq(0, 75, length.out = 16)
terra::freq(fev_data(fev_danger_class(r)))
terra::freq(fev_data(fev_danger_class(r, "caliver_europe")))

}
\seealso{
\code{\link[=fev_fwi_classes]{fev_fwi_classes()}} for the schemes and how they differ,
\code{\link[=fev_fwi_thresholds]{fev_fwi_thresholds()}} to derive breaks from a record.
}
