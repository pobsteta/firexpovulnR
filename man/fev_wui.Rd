% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wui.R
\name{fev_wui}
\alias{fev_wui}
\title{The wildland-urban interface}
\usage{
fev_wui(
  assets,
  fuel,
  distance = 100,
  assets_min = 0,
  side = c("assets", "fuel", "both"),
  lookup = NULL,
  quiet = FALSE
)
}
\arguments{
\item{assets}{A raster of exposed assets: a \link{fev_source} from
\code{\link[=fev_fetch_ghsl]{fev_fetch_ghsl()}}, a \code{fev_layer}, or a \code{SpatRaster}. Cells above
\code{assets_min} count as present.}

\item{fuel}{A \link{fev_fuel_source}, \code{fev_layer} or \code{SpatRaster}, reduced to a
burnable mask exactly as \code{\link[=fev_exposure]{fev_exposure()}} reduces it.}

\item{distance}{Interface distance, in CRS units. Default 100 m, the
short-range ember scale of \code{\link[=fev_exposure_radii]{fev_exposure_radii()}} and the order of the
French \emph{obligation légale de débroussaillement}. It is a parameter, not a
constant: pass what your context justifies.}

\item{assets_min}{Threshold above which an assets cell counts as present.
Default \code{0}, i.e. any non-zero value. With a modelled population surface
that is a low bar — GHS-POP spreads fractions of a person over cells with a
trace of built-up — so raise it when the result looks implausibly wide.}

\item{side}{\code{"assets"}, \code{"fuel"} or \code{"both"}.}

\item{lookup}{Passed to the fuel reduction, as in \code{\link[=fev_exposure]{fev_exposure()}}.}

\item{quiet}{Suppress the report.}
}
\value{
A \code{fev_layer} of role \code{"wui"}, values 1 on the interface and 0
elsewhere, ready to enter \code{\link[=fev_risk]{fev_risk()}} as a further dimension.
}
\description{
Where people and burnable vegetation are close enough that one reaches the
other: cells holding assets with fuel within \code{distance}, and optionally the
converse — fuel with assets within \code{distance}.
}
\section{Which definition this is}{

There is no single WUI. The literature splits broadly into a \strong{housing
density} definition (the US federal register's interface and intermix,
thresholds on structures per unit area) and a \strong{proximity} definition
(anything built within a set distance of wildland). This function implements
the second, because it is the one the package's inputs can actually support:
GHS-POP gives a modelled population surface, not a structure count, and
counting structures per hectare from it would be inventing a precision the
source does not carry.

The consequence is worth stating plainly: this identifies \emph{proximity}, not
\emph{risk to a dwelling}. A single isolated house 80 m from maquis and a dense
hamlet 80 m from the same maquis both come out as interface, and the second
is a far larger problem.
}

\section{Distance is measured through the window, not around obstacles}{

The neighbourhood is a disc of radius \code{distance}, so a river, a motorway or a
cliff between the assets and the fuel counts for nothing. Fire does cross
most of those; embers cross all of them. That is the argument for the simple
version, and it is also the reason not to read the output as a fire-break
analysis.
}

\section{Two sides, and why both are offered}{

\code{side = "assets"} returns the exposed assets — what a mayor asks for. \code{"fuel"}
returns the fuel that threatens them — what a forester asks for, since that
is where clearing obligations apply. \code{"both"} returns their union. The three
answer different questions and the record says which was asked.
}

\examples{
g <- terra::rast(nrows = 20, ncols = 20, xmin = 0, xmax = 200,
                 ymin = 0, ymax = 200, crs = "EPSG:2154")
# Fuel on the left half, one inhabited cell just across the divide.
fuel <- terra::setValues(g, rep(rep(c(1, 0), each = 10), 20))
pop <- terra::setValues(g, 0)
pop[10, 12] <- 50
wui <- fev_wui(pop, fuel, distance = 30, quiet = TRUE)
terra::global(fev_data(wui), "sum", na.rm = TRUE)

}
\seealso{
\code{\link[=fev_fetch_ghsl]{fev_fetch_ghsl()}} for an assets source, \code{\link[=fev_exposure]{fev_exposure()}} for the
graded metric this complements, \code{\link[=fev_risk]{fev_risk()}} to combine them.
}
