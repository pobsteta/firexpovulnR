% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downscale_wind_rain.R
\name{fev_curvature}
\alias{fev_curvature}
\title{Terrain curvature on the MicroMet scale}
\source{
Liston, G. E. and Elder, K. (2006). A meteorological distribution system for
high-resolution terrestrial modeling (MicroMet). \emph{Journal of
Hydrometeorology} 7(2): 217-234.

Formulation checked on 2026-08-19 against an independent implementation
rather than taken from memory: \code{windwt = 1 + slopewt * wind_slope + curvewt * curvature}, with both terms divided by twice their maximum absolute value and
\code{slopewt + curvewt = 1} suggested. The publisher's own page is paywalled.
}
\usage{
fev_curvature(dem, length_scale = 500, quiet = FALSE)
}
\arguments{
\item{dem}{Elevation: a \link{fev_source} from \code{\link[=fev_fetch_dem]{fev_fetch_dem()}}, a \code{fev_layer} or
a \code{SpatRaster}.}

\item{length_scale}{Distance in CRS units over which curvature is measured.
Default 500.}

\item{quiet}{Suppress the report.}
}
\value{
A \code{fev_layer} of role \code{"curvature"}, values in \verb{[-0.5, 0.5]}.
}
\description{
Convex ground — ridges, spurs — accelerates wind; concave ground shelters it.
This computes the curvature that \code{\link[=fev_downscale_weather]{fev_downscale_weather()}} turns into a wind
multiplier, on the scale Liston and Elder define: divided by twice its own
maximum absolute value, so it lands in \verb{[-0.5, 0.5]}.
}
\section{The length scale is the parameter that matters}{

Curvature has no meaning without a distance over which to measure it. Liston
and Elder use roughly half the dominant topographic wavelength — the distance
from a valley floor to the ridge above it. Too short and the result is
surface roughness; too long and a whole massif reads as one gentle dome.
Several hundred metres suits a Mediterranean massif; the default is 500 m and
it is a choice, not a constant.
}

\section{It is scaled against the area processed}{

Dividing by the observed maximum makes the result \strong{extent-dependent}, like
\code{"minmax"} in \code{\link[=fev_vuln_layer]{fev_vuln_layer()}}: the same ridge scores differently under a
different area of interest. That is how the published scheme is defined, and
the consequence is the same one — a curvature layer is comparable within one
analysis and not between two.
}

\examples{
g <- terra::rast(nrows = 30, ncols = 30, xmin = 0, xmax = 3000,
                 ymin = 0, ymax = 3000, crs = "EPSG:2154")
# A ridge running north-south.
dem <- terra::init(g, "x")
dem <- 300 - abs(dem - 1500) / 5
cv <- fev_curvature(dem, length_scale = 300, quiet = TRUE)
range(terra::values(fev_data(cv)), na.rm = TRUE)

}
\seealso{
\code{\link[=fev_downscale_weather]{fev_downscale_weather()}}, which consumes it.
}
