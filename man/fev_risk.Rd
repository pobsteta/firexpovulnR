% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{fev_risk}
\alias{fev_risk}
\title{Composite fire risk}
\source{
CLIMAAX Climate Risk Assessment Handbook, FWI wildfire workflow, read
2026-08-16:
\url{https://handbook.climaax.eu/notebooks/workflows/FIRE/02_wildfire_FWI/FWI_Risk_Assessment.html}.
The min-max-then-average step and the \verb{paretoset(..., sense = [max, ...])}
call were read from the notebook's own code, not from its prose.
}
\usage{
fev_risk(
  danger,
  vulnerability,
  ...,
  method = c("effis_mean", "pareto", "weighted"),
  weights = NULL,
  normalise = c("minmax", "none"),
  depth = 1,
  digits = 3,
  max_unique = 50000
)
}
\arguments{
\item{danger}{Composite danger: a \code{fev_layer} from \code{\link[=fev_danger_index]{fev_danger_index()}}, or a
\code{SpatRaster}.}

\item{vulnerability}{Vulnerability: a \code{fev_layer} from \code{\link[=fev_vuln_stack]{fev_vuln_stack()}}, or
a \code{SpatRaster}.}

\item{...}{Further named dimensions to enter the combination, as CLIMAAX does
with its wildland-urban interface, protected area, ecological
irreplaceability and restoration cost indicators.}

\item{method}{\code{"effis_mean"}, \code{"pareto"} or \code{"weighted"}.}

\item{weights}{Required for \code{"weighted"}: one per dimension, summing to 1.}

\item{normalise}{\code{"minmax"} (default, as CLIMAAX) or \code{"none"}.}

\item{depth}{For \code{"pareto"}: how many successive fronts to peel. \code{1} gives
the CLIMAAX boolean front; a larger number gives a graded layer where the
first front scores 1 and deeper fronts score less; \code{Inf} peels until
nothing is left.}

\item{digits}{For \code{"pareto"}: values are rounded to this many decimals before
dominance is computed. A front computed on floating-point noise in the
fifteenth decimal is not a front, and rounding is what keeps the problem
tractable.}

\item{max_unique}{For \code{"pareto"}: refuse rather than run for hours when the
rounded values yield more distinct combinations than this.}
}
\value{
A \code{fev_risk_layer} holding a \code{SpatRaster} named \code{risk}.
}
\description{
Crosses composite danger with vulnerability, and optionally with further
dimensions, into one risk layer.
}
\section{The three methods answer different questions}{

\code{"effis_mean"} normalises every component with min-max and takes their
unweighted arithmetic mean. This is the CLIMAAX danger-index step applied to
risk: simple, continuous, and it lets a very high score on one dimension be
bought back by a low score on another. That compensation is the whole
objection to it.

\code{"pareto"} is what the CLIMAAX wildfire workflow actually does for risk. It
returns the \strong{Pareto front}: the cells that cannot be improved on one
dimension without being worse on another, treating every dimension as
equally important with no weights at all. Note what this gives you — a
\strong{set}, not a score. A cell is on the front or it is not, and the front says
nothing about how far from it the others are. Use \code{depth} to peel successive
fronts and get a graded layer instead.

\code{"weighted"} is a weighted mean. It requires \code{weights} and writes them into
the record, because they are the analysis's judgement and nothing in the data
supplies them.
}

\section{Normalisation restretches layers that were already scaled}{

\code{normalise = "minmax"} is the default because it is what CLIMAAX does. It
rescales each component on its own observed range, so a
\code{\link[=fev_vuln_stack]{fev_vuln_stack()}} layer that legitimately spans 0.3 to 0.6 comes out
spanning 0 to 1, and its lowest cell becomes a zero. When your components are
already on a meaningful common scale — which is what the rest of this package
produces — pass \code{normalise = "none"}.

Min-max also makes the result depend on the extent processed: the same cell
scores differently under a different AOI.
}

\section{Grids must already match}{

As everywhere outside \code{\link[=fev_align]{fev_align()}}, mismatched grids are an error.
}

\examples{
g <- terra::rast(nrows = 6, ncols = 6, xmin = 0, xmax = 150,
                 ymin = 0, ymax = 150, crs = "EPSG:2154")
danger <- terra::setValues(g, seq(0, 1, length.out = 36))
vuln <- terra::setValues(g, rev(seq(0, 1, length.out = 36)))

mean_risk <- fev_risk(danger, vuln, normalise = "none")
terra::global(fev_data(mean_risk), "mean", na.rm = TRUE)

front <- fev_risk(danger, vuln, method = "pareto", normalise = "none")
terra::global(fev_data(front), "sum", na.rm = TRUE)

}
\seealso{
\code{\link[=fev_validate]{fev_validate()}} to test the result against burnt areas,
\code{\link[=fev_align]{fev_align()}} to put the inputs on one grid.
}
