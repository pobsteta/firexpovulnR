% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lidar_batch.R
\name{fev_lidar_batch}
\alias{fev_lidar_batch}
\title{Invert LiDAR HD tiles in batch, with resume}
\usage{
fev_lidar_batch(
  aoi,
  out_dir,
  res = 25,
  window = NULL,
  max_tiles = NULL,
  spread = TRUE,
  keep_las = FALSE,
  dry_run = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{aoi}{Area of interest, or an \code{fev_lidarhd_index} from
\code{\link[=fev_lidarhd_available]{fev_lidarhd_available()}}.}

\item{out_dir}{Directory for the per-tile rasters and the manifest. Created
if absent.}

\item{res}{Target cell size in metres, passed to \code{\link[=fev_fuel_lidar]{fev_fuel_lidar()}}.}

\item{window}{Side of a centred square to process within each tile, in
metres. \code{NULL} processes the whole tile.}

\item{max_tiles}{Stop after this many tiles in one run — a way to take an
overnight job in shifts. \code{NULL} for no limit.}

\item{spread}{Choose those tiles spread across the area rather than in index
order. On by default, and it matters: the index is in spatial order, so the
first eight tiles of it are eight neighbours in one corner. See the section
below.}

\item{keep_las}{Keep the downloaded point clouds instead of deleting each one
after its tile is inverted. Off by default: at roughly 200 MB a tile, a
departmental run would fill a disk.}

\item{dry_run}{Report what would be done and return the plan without
downloading anything.}

\item{quiet}{Suppress progress reporting.}
}
\value{
A data frame, one row per tile: \code{tile}, \code{status}, \code{points},
\code{seconds}, \code{path}, \code{rank}. \code{status} is \code{"done"} for a tile whose raster
already exists, \code{"next"} for one this run will take, \code{"todo"} for one left
for a later run, and \code{"written"} or \code{"failed"} afterwards. \code{rank} gives the
position in the traversal, so a dry run shows the actual batch rather than
the index order. Also written to \code{manifest.csv} in \code{out_dir} after every
tile, so an interrupted run leaves a readable record.
}
\description{
Walks a set of LiDAR HD tiles one at a time: downloads, inverts to fuel
metrics with \code{\link[=fev_fuel_lidar]{fev_fuel_lidar()}}, writes one raster per tile, and deletes the
point cloud before moving on. Interrupt it whenever you like and start it
again — it picks up where it stopped.
}
\section{Why resume is the point}{

Measured on real tiles over the Maures, none of these finished: 3.0 M points
in over 25 minutes, 11.1 M in over 40, a whole tile in over 90. Downloading
is not the bottleneck — a 248 MB tile arrives in 42 seconds — the inversion
is. A departmental run is therefore an overnight batch job, and any batch job
that cannot be interrupted will be, at the worst moment.

Resume is by \strong{output presence}, not by a state file that can drift from the
truth: a tile whose raster is on disk is done. Each raster is written to a
temporary name and renamed only once complete, so an interrupted write is
never mistaken for a finished tile.
}

\section{What \code{window} is for}{

A full tile is a square kilometre and, at LiDAR HD's density, tens of
millions of points. \code{window} processes a centred square of \code{window} metres
instead, which is what makes a spread of tiles affordable: eight 250 m
windows across a massif sample eight contexts, where one whole tile samples
one, for a fraction of the cost.

Spread beats contiguity for anything you intend to compare against a
classification — which is what \code{\link[=fev_fuel_profile]{fev_fuel_profile()}} does.
}

\section{Why the order is not the index order}{

\code{max_tiles} without \code{spread} would hand you the first N tiles of an index
that is itself in spatial order — eight neighbours in one corner of the
massif, sampling one context eight times. \code{spread = TRUE} walks the tiles by
farthest-point traversal instead: each tile chosen is the one furthest from
everything chosen so far.

The traversal is deterministic and computed over the whole set, so every
prefix of it is well spread \strong{and} resume continues the spread rather than
re-drawing it. Eight tiles tonight and eight tomorrow give sixteen spread
tiles, not two clusters of eight.
}

\section{Do not thin the cloud to go faster}{

It is the obvious optimisation and it destroys the measurement. As pulse
density falls the understorey stratum is the first thing to disappear, so
thinning biases exactly the quantity these metrics exist to carry. Reduce
\code{window}, or process fewer tiles; never reduce the density. See
\code{\link[=fev_lidar_density]{fev_lidar_density()}}.
}

\examples{
\dontrun{
study <- sf::st_read("massif_maures.gpkg")

# What it would do, without downloading anything.
fev_lidar_batch(study, "out/lidar", window = 250, dry_run = TRUE)

# Overnight, in shifts. Run it again tomorrow and it continues.
fev_lidar_batch(study, "out/lidar", window = 250, max_tiles = 8)
}

}
\seealso{
\code{\link[=fev_fuel_lidar]{fev_fuel_lidar()}} for the inversion and what it costs,
\code{\link[=fev_lidarhd_available]{fev_lidarhd_available()}} for the coverage check.
}
