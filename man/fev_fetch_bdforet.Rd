% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_bdforet.R
\name{fev_fetch_bdforet}
\alias{fev_fetch_bdforet}
\title{Fetch BD Forêt v2 (primary fuel source)}
\usage{
fev_fetch_bdforet(
  aoi,
  millesime = NA,
  dept = NULL,
  crs_work = 2154,
  cache = TRUE,
  layer = .FEV_LAYERS$bdforet_v2
)
}
\arguments{
\item{aoi}{Area of interest: \code{sf}, \code{sfc}, \code{bbox}, \code{SpatVector} or
\code{SpatRaster}. Must carry a CRS.}

\item{millesime}{Vintage of the data, as a year, or a data frame with
columns \code{dept} and \code{year} for a multi-department AOI. \code{NA} by default
because the source does not provide it — supply it rather than let it
default.}

\item{dept}{Optional department code(s) to record in provenance. Purely
documentary: filtering is done by \code{aoi}.}

\item{crs_work}{EPSG code to return the data in. Default \code{2154}, BD Forêt's
native CRS — leaving it there avoids reprojecting the categorical primary
source.}

\item{cache}{Use the on-disk cache. See \code{\link[=fev_cache_dir]{fev_cache_dir()}}.}

\item{layer}{WFS layer name. Defaults to the verified BD Forêt v2 layer;
override only if IGN moves it.}
}
\value{
A \link{fev_source} holding an \code{sf} of vegetation formations with
columns \code{id}, \code{code_tfv}, \code{tfv}, \code{tfv_g11}, \code{essence}.
}
\description{
Downloads vegetation formation polygons from the IGN Géoplateforme WFS for
an area of interest. BD Forêt v2 is the package's \strong{primary fuel source}:
its 0.5 ha minimum mapping unit and 20 m minimum width are what make
hectometric focal exposure meaningful, where CORINE's 25 ha unit is not.
}
\section{Access route}{

\code{geoservices.ign.fr} was retired in March 2026 and now redirects to
cartes.gouv.fr, which documents no programmatic route. This function uses
the Géoplateforme WFS instead — no authentication, filtered server-side by
AOI, so only the study area is transferred rather than whole departments.
Pagination and the IGN axis-order quirk are handled by \code{happign}.
}

\section{On the vintage}{

\strong{The WFS does not serve the vintage.} It is absent from the layer schema,
and no per-department table was found at IGN or in any related project. BD
Forêt v2 was built department by department between 2007 and 2018, so a
national assembly is not a snapshot: a stand mapped in 2008 may have burnt
in 2010 and be in a completely different state.

\code{millesime} is therefore recorded as \code{NA} unless you supply it, and nothing
is inferred. Supply it when you know it — \code{fev_fuel_source()} requires it,
and the temporal-bias check in \code{fev_validate()} cannot run without it.
}

\examples{
\dontrun{
aoi <- sf::st_read("massif_maures.gpkg")
bdf <- fev_fetch_bdforet(aoi, millesime = 2014, dept = "83")
bdf
table(fev_data(bdf)$code_tfv)
}

}
\seealso{
\code{\link[=fev_fetch_corine]{fev_fetch_corine()}} for the auxiliary source,
\code{fev_fuel_source()} to turn this into a fuel layer.
}
