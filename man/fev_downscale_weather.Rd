% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/danger_downscale.R
\name{fev_downscale_weather}
\alias{fev_downscale_weather}
\title{Correct weather to the elevation of each topographic zone}
\usage{
fev_downscale_weather(weather, zones, lapse_rate = 6.5, quiet = FALSE)
}
\arguments{
\item{weather}{A weather table as \code{\link[=fev_fetch_weather]{fev_fetch_weather()}} returns, or a
\link{fev_source} holding one. Needs \code{elev_m}, the elevation the reanalysis
thinks each point sits at, without which there is nothing to correct
\emph{from}.}

\item{zones}{A \code{fev_layer} from \code{\link[=fev_topo_zones]{fev_topo_zones()}}.}

\item{lapse_rate}{Kelvin per kilometre, positive for cooling with height.
Default 6.5.}

\item{quiet}{Suppress the report.}
}
\value{
A data frame in \code{\link[=fev_fwi_calc]{fev_fwi_calc()}}'s input shape, one \code{id} per zone,
with \code{elev_m} set to the zone's mean elevation and a \code{zone} column.
}
\description{
Produces one weather series per zone, with temperature moved by the
environmental lapse rate and relative humidity following from it at constant
dewpoint. The result is the exact shape \code{\link[=fev_fwi_calc]{fev_fwi_calc()}} expects, so the FWI
accumulates per zone from corrected inputs rather than being adjusted after
the fact.
}
\section{What is corrected and what is not}{

\strong{Temperature} moves with the lapse rate. \strong{Humidity} follows, because
cooling air at constant water content raises its relative humidity — that is
thermodynamics, not a fitted relation.

\strong{Wind and rain are passed through unchanged.} Topographic exposure and
orographic enhancement are both real and neither has a calibration this
package can defend on a Mediterranean massif. Inventing a multiplier for
either would put a number in the provenance that nobody could justify. The
consequence is precise and must be read: ISI keeps the coarse wind signal,
DMC and DC keep the coarse rain signal, and it is the fine-fuel path — FFMC,
and FWI through it — that gains structure.
}

\section{A percentile normalisation downstream will undo all of this}{

Measured on the Maures for 16 August 2021, over 597 840 cells, decomposing
the composite risk into its two terms:\tabular{llll}{
   chain \tab sd(danger) \tab cor(risk, danger) \tab cor(risk, vulnerability) \cr
   coarse, percentile \tab 0.032 \tab 0.542 \tab \strong{0.995} \cr
   downscaled, percentile \tab 0.0006 \tab 0.380 \tab \strong{1.000} \cr
   coarse, raw FWI \tab 0.237 \tab 0.699 \tab 0.769 \cr
   downscaled, raw FWI \tab \strong{0.265} \tab \strong{0.807} \tab 0.808 \cr
}


Read the first two rows: downscaling made the composite \strong{worse}. And the
reason is not the downscaling.

\code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}} ranks each cell against \strong{its own} history. It is a
temporal normalisation, so it removes spatial contrast by construction: a
place that is always hot and a place that is always cool both come out near
1 on an extreme day. What little spatial variation survived it in the coarse
chain was an artefact of reanalysis cells having different histories across
their block boundaries — and downscaling, by giving every zone a coherent
series of its own, removes that artefact too.

So the percentile is doing its job, and its job is not compatible with
wanting a danger term that varies in space on a given day. Downscale and
normalise with \code{"minmax"}, or downscale and accept that the percentile map
answers a different question — \emph{how unusual is today here} rather than
\emph{where is it worst today}. Both are legitimate; they are not the same map,
and the composite built from the second is the one that stops being its own
exposure layer.
}

\section{The lapse rate is a constant and the atmosphere is not}{

6.5 K/km is the ICAO standard mean. The real rate runs from about 9.8 K/km in
dry unstable air to near zero, and \strong{inverts} during Mediterranean summer
nights, when cold air pools in the valleys and the slope above is warmer than
the floor. The FWI takes noon conditions, when inversions have usually broken,
which is the argument for using a mean rate here — not that it is always
right, but that it is right at the hour the index reads.

Pass your own \code{lapse_rate} if you have one for your season and site. It is
recorded either way.
}

\examples{
\dontrun{
dem <- fev_fetch_dem(zone)
z <- fev_topo_zones(fev_data(dem))
w <- fev_downscale_weather(meteo, z)
danger <- fev_fwi_zonal(w, z, dates = as.Date("2021-08-16"))
}

}
\seealso{
\code{\link[=fev_topo_zones]{fev_topo_zones()}}, \code{\link[=fev_fwi_zonal]{fev_fwi_zonal()}} to run the FWI on the result.
}
