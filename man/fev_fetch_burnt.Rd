% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_burnt.R
\name{fev_fetch_burnt}
\alias{fev_fetch_burnt}
\title{Fetch EFFIS burnt area perimeters}
\usage{
fev_fetch_burnt(
  aoi,
  period = NULL,
  file = NULL,
  crs_work = 2154,
  cache = TRUE,
  layer = .FEV_LAYERS$effis_ba
)
}
\arguments{
\item{aoi}{Area of interest: \code{sf}, \code{sfc}, \code{bbox}, \code{SpatVector} or
\code{SpatRaster}. Must carry a CRS.}

\item{period}{Two-element character or numeric vector giving the first and
last year (or dates) to keep, e.g. \code{c("2016", "2025")}. \code{NULL} keeps
everything returned.}

\item{file}{Optional path to a local burnt-area file, for extracts obtained
through the EFFIS data request form.}

\item{crs_work}{EPSG code to return the data in. Default \code{2154}.}

\item{cache}{Use the on-disk cache.}

\item{layer}{WFS type name. Defaults to the verified \code{ms:modis.ba.poly}.}
}
\value{
A \link{fev_source} holding an \code{sf} of burnt area polygons, with at
least \code{FIREDATE}, \code{AREA_HA} and \code{COUNTRY}. A \code{fire_year} column is added
for convenience.
}
\description{
Downloads burnt area polygons from the EFFIS/GWIS service, for validating a
risk surface against what actually burnt.
}
\section{Coverage is shorter than you may expect}{

The public EFFIS endpoint serves roughly \strong{2016 onward} — not 2006. EFFIS
states that historic data, database extracts and raw perimeters go through
their data request form instead. This function therefore compares the
period you asked for against the period actually returned, and warns with
numbers rather than a vague caveat: silently validating on ten years while
believing you validated on eighteen is the failure mode worth preventing.

Use \code{file} to bring in an extract obtained through the EFFIS request form.
}

\section{The CRS is forced, deliberately}{

EFFIS ships a \code{.prj} declaring \code{GCS_unknown} although the coordinates are
plain WGS84, so \code{sf::st_crs()} reads \code{unknown}. The package sets EPSG:4326
explicitly and records the override in provenance. It is never applied
silently, because "the provider mislabels its CRS" is exactly the kind of
assumption that must be visible in a methods section.
}

\examples{
\dontrun{
aoi <- sf::st_read("massif_maures.gpkg")
ba <- fev_fetch_burnt(aoi, period = c("2016", "2025"))
fev_source_info(ba)$period_returned
}

}
\seealso{
\code{fev_validate()}, which uses these to score a risk surface and
runs the temporal-bias check against the fuel vintage.
}
