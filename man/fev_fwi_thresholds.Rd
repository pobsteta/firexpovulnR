% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fwi_percentile.R
\name{fev_fwi_thresholds}
\alias{fev_fwi_thresholds}
\title{Danger thresholds derived from a climatology}
\source{
caliver, \code{R/get_fire_danger_levels.R}, ecmwf/caliver on GitHub, read
2026-08-16. Method published as Vitolo, C., Di Giuseppe, F., D'Andrea, M.
(2018), \emph{Caliver: An R package for CALIbration and VERification of forest
fire gridded model outputs}, PLoS ONE 13(1): e0189419.
\doi{10.1371/journal.pone.0189419}. Applied there to ERA-Interim 1980–2016
over the April–October season, the published European result is
\verb{2, 5, 10, 19, 33}.
}
\usage{
fev_fwi_thresholds(x, ndays = 4, season = c("04-01", "10-31"), time = NULL)
}
\arguments{
\item{x}{A \code{SpatRaster} of daily fire danger, or a numeric vector with a
\code{years} attribute, or a \code{fev_source}.}

\item{ndays}{Days per year on which a fire is expected. caliver's default is
4, which yields the 98th percentile.}

\item{season}{Fire season, as in \code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}}.}

\item{time}{Dates of the layers, when \code{x} does not carry them.}
}
\value{
A numeric vector of five breaks, with the derived extreme value in
its \code{extreme} attribute. Feed it to \code{\link[=fev_fwi_classes]{fev_fwi_classes()}}.
}
\description{
Reimplements caliver's \code{get_fire_danger_levels()}: the five interior FWI
breaks separating six danger classes, derived from the record itself rather
than imported.
}
\section{The algorithm, and where its constants come from}{

For each year, the \code{(1 - ndays/365)} quantile of the fire index is taken —
with the default \code{ndays = 4} that is the 98th percentile, read as "the level
exceeded on the four days a year a fire is expected". The \strong{median across
years} of those yearly extremes is the extreme danger value. It is then
inverted through the Canadian system's intensity relation to a fire
intensity, and the six classes fall out of a geometric progression of that
intensity.

The numeric constants (\code{0.289}, \code{0.980}, \code{1.546}, \code{1.013}, \code{0.647}) are
caliver's, read from its source. They belong to the Canadian FWI-intensity
relationships. This package has \strong{not} verified them against Van Wagner's
original report — it reproduces a published implementation, and says so.
}

\section{What it is not}{

This is not \code{\link[=fev_fwi_percentile]{fev_fwi_percentile()}}. That one asks "how unusual is today
here"; this one asks "where should the class boundaries sit for this
record". caliver's own documentation insists on the distinction, and mixing
them produces a map that is neither.
}

\examples{
r <- terra::rast(nrows = 2, ncols = 2, xmin = 0, xmax = 50,
                 ymin = 0, ymax = 50, crs = "EPSG:2154", nlyr = 40)
set.seed(1)
terra::values(r) <- runif(160, 0, 60)
terra::time(r) <- seq(as.Date("2019-06-01"), by = "day", length.out = 40)
fev_fwi_thresholds(r, season = NULL)

}
