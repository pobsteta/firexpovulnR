% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdforet_millesime.R
\name{fev_bdforet_millesime}
\alias{fev_bdforet_millesime}
\title{BD Forêt v2 vintage, from the imagery it was photo-interpreted on}
\source{
IGN, \emph{BD Forêt® Version 2 — Descriptif de contenu}, September 2014, §2.3,
\url{https://inventaire-forestier.ign.fr/IMG/pdf/DC_BDFORET_v2.pdf}, read
2026-08-16. Mosaicking graph layers and their \code{date_vol} field verified by
\code{DescribeFeatureType} and by real \code{GetFeature} requests over the Var on the
same day.
}
\usage{
fev_bdforet_millesime(
  aoi,
  period = .FEV_BDFORET_V2_WINDOW,
  strategy = NULL,
  crs_work = 2154,
  cache = TRUE
)
}
\arguments{
\item{aoi}{Area of interest: \code{sf}, \code{sfc}, \code{bbox}, \code{SpatVector} or
\code{SpatRaster}. Must carry a CRS.}

\item{period}{Two years bounding the search. Defaults to the BD Forêt v2
construction window, 2007 to 2018.}

\item{strategy}{\code{NULL} (default) returns every candidate. \code{"oldest"} and
\code{"newest"} collapse to a single year, and say which rule produced it.}

\item{crs_work}{EPSG code to work in. Default \code{2154}.}

\item{cache}{Use the on-disk cache. See \code{\link[=fev_cache_dir]{fev_cache_dir()}}.}
}
\value{
With \code{strategy = NULL}, a data frame of class \code{fev_millesime} with
one row per campaign: \code{dep}, \code{pva}, \code{date_vol}, \code{year}, \code{n_polygons} and
\code{pct_area} — the share of the mosaic area over the AOI that campaign
covers. With a strategy, a single integer year carrying the table as its
\code{candidates} attribute.
}
\description{
Retrieves the aerial survey dates of the BD ORTHO campaigns covering an area
of interest, which is what IGN defines the BD Forêt v2 validity date to be.
}
\section{Why this is the vintage, not a proxy for it}{

The BD Forêt v2 content description states it directly: \emph{« La date de
validation est celle de la prise de vues de la BD ORTHO® servant à la
production des données. »} The database describes the stand as the
photo-interpreter saw it on the infrared imagery, so the survey date is the
date of the landscape state it records — which is exactly what a temporal
bias check needs, and arguably better than a publication date would be.

The WFS serving BD Forêt v2 carries no date field of any kind (schema
verified 2026-08-16: \code{id}, \code{code_tfv}, \code{tfv}, \code{tfv_g11}, \code{essence},
\code{geom}), so this is the only route to it that does not involve guessing.
}

\section{It usually returns more than one candidate}{

BD ORTHO reflies a department roughly every five years, and BD Forêt v2 was
built between 2007 and 2018. Most departments therefore have two campaigns
inside that window — the Var has 2008 and 2014 — and \strong{IGN does not publish
which one fed which department's production}. This function will not choose
for you.

Two things narrow it. Full metropolitan coverage was planned for early 2016,
so a survey flown after that fed an update rather than an initial
production. And \code{strategy = "oldest"} is the conservative reading: assuming
the older campaign maximises the lag the validation reports, which errs
toward flagging temporal bias rather than hiding it.
}

\examples{
\dontrun{
aoi <- sf::st_read("massif_maures.gpkg")

# Every campaign that could have fed the BD Forêt v2 of this area:
fev_bdforet_millesime(aoi)

# Collapsed conservatively, for an automated pipeline:
millesime <- fev_bdforet_millesime(aoi, strategy = "oldest")
bdf <- fev_fetch_bdforet(aoi, millesime = millesime)
}

}
\seealso{
\code{\link[=fev_fetch_bdforet]{fev_fetch_bdforet()}}, which accepts \code{millesime = "auto"} to call
this, and \code{\link[=fev_validate]{fev_validate()}}, which is what needs the answer.
}
